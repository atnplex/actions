name: "AI Review Helper"
description: "Template for AI-assisted PR reviews"

on:
  workflow_call:
    inputs:
      model:
        description: "Model to use"
        default: "gemini-pro"
        type: string

jobs:
  ai-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: AI Review Placeholder
        shell: bash
        run: |
          echo "AI Review Step"
          echo "To enable actual AI review, configure an API key and call an LLM provider here."
          echo "Model selected: ${{ inputs.model }}"
